# 📅 Devlog — May 6, 2026

## 🧠 Focus for Today

- VPS security hardening
- SSH configuration and deployment reliability
- Improve long-term Linux server administration workflow

---

## ✅ Tasks Completed

### 🔐 VPS — SSH Port Migration & Hardening

- Migrated VPS SSH access away from default port 22 to a custom SSH port
- Updated `/etc/ssh/sshd_config` with custom port configuration
- Validated SSH configuration using `sshd -t` before restarting service
- Restarted SSH service safely while keeping original SSH session active
- Opened a second terminal session to verify successful login on the new SSH port before disconnecting original session
- Diagnosed SSH socket activation issue where `ssh.socket` continued binding to port 22
- Disabled `ssh.socket` and transitioned fully to standard `ssh.service` management
- Verified SSH daemon listening correctly on custom port using `ss`

---

### 🔥 UFW Firewall Cleanup

- Added new custom SSH port rule to UFW before migration
- Verified firewall configuration using:
  - `ufw status`
  - `ufw status numbered`
- Removed legacy:
  - `OpenSSH`
  - `OpenSSH (v6)`
    firewall rules after confirming successful migration
- Retained Apache Full firewall rules for ports 80 and 443

---

### 📦 Node.js Installation

- Installed Node.js LTS using the official NodeSource repository
- Installed npm (Node Package Manager)
- Learned the purpose of:
  - `curl`
  - NodeSource repositories
  - LTS (Long Term Support) software releases
- Verified Node.js and npm installation successfully
- Created and executed initial `test.js` file to confirm Node.js runtime functionality

---

### 🚀 GitHub Actions — Deployment Fixes

- Diagnosed GitHub Actions deployment failures caused by workflows still targeting port 22
- Added new repository secret for custom VPS SSH port
- Updated deployment workflow YAML files to use custom SSH port secret
- Successfully verified automated deployments for:
  - benkelly.design
  - kralbenji.com
- Confirmed deployment pipelines function correctly after SSH migration

---

## 💡 Notes / Observations

- Keeping the original SSH session open while testing the new port from a second terminal prevented accidental VPS lockout
- UFW firewall rules and SSH daemon configuration are separate systems and both must be configured correctly during SSH port migration
- Apache virtual hosts, SSL certificates, domains, and website traffic remained unaffected during the SSH migration process

---

## 🚀 Next Steps

- Continue refining Linux VPS administration workflow
- Explore additional VPS hardening and monitoring tools
- Continue improving automated deployment workflow and infrastructure knowledge
